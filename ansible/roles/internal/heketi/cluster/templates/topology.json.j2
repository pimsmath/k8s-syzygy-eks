#jinja2: trim_blocks:False
{
    "clusters": [
        {
            "nodes": [{% for client in groups['heketi-cluster'] %}
                {
                    "node": {
                        "hostnames": {
                            "manage": [
                                "{{ hostvars[client].ansible_eth0.ipv4.address }}"
                            ],
                            "storage": [
                                "{{ hostvars[client].ansible_eth0.ipv4.address }}"
                            ]
                        },
                        "zone": 1
                    },
                    "devices": [
                        "/dev/sdb"
                    ]
                }{% if not loop.last %},{% endif %}{% endfor %}
            ]
        }
    ]
}
